// Havemind Panels — Claude Design canvas runtime placeholder.
//
// The real `support.js` for this Claude Design document is the Claude Design
// React canvas runtime (dc-runtime). It is the render harness for the .dc.html
// canvas preview only — it is NOT part of the Havemind Obsidian plugin and must
// NOT be ported: the plugin builds its UI with Obsidian's `createEl` / `setIcon`
// APIs, not React.
//
// This file could not be re-fetched via the DesignSync MCP tool in the session
// that produced the implementation (the MCP tool was not exposed to the coding
// agent; the design HTML was recovered from a cached MCP result on disk). The
// runtime is not needed to implement the design — `Havemind Panels.dc.html`
// carries the full component markup and inline styles. Kept as a placeholder so
// the reference folder documents both design artefacts.
