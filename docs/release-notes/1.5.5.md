# Havemind 1.5.5

- Fixes phones and tablets that stopped receiving updates after another device
  changed an attachment such as a PDF. The device showed "offline" and never
  caught up. Updates now arrive normally.

Privacy: Havemind makes network requests only to the self-hosted server URL a
user explicitly configures for a vault. It contains no telemetry or analytics.
