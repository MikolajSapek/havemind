# Havemind 1.4.5

- The Activity tab prepares only the rows it shows. It was building the display
  text for the whole log and throwing most of it away on every repaint.

Privacy: Havemind makes network requests only to the self-hosted server URL a
user explicitly configures for a vault. It contains no telemetry or analytics.
