# Havemind 1.4.18

People on a phone now shows every active member of the vault, not only "You".
Connecting no longer lets a local self-seed overwrite the server roster, and the
live sync connection is enough to refresh the list.

Privacy: Havemind makes network requests only to the self-hosted server URL a
user explicitly configures for a vault. It contains no telemetry or analytics.
