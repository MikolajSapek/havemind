# Havemind 1.4.19

Joining a shared vault from an empty phone no longer fills Havemind Conflicts
with false "Target unknown" copies. Bootstrap adopts each server head into the
producer map before reconcile runs.

Privacy: Havemind makes network requests only to the self-hosted server URL a
user explicitly configures for a vault. It contains no telemetry or analytics.
