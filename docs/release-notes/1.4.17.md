# Havemind 1.4.17

Joining or catching up a vault no longer waits for one blob download to finish
before starting the next. Current file heads are applied several at a time, so a
mostly-text vault on a phone lands much faster.

Privacy: Havemind makes network requests only to the self-hosted server URL a
user explicitly configures for a vault. It contains no telemetry or analytics.
