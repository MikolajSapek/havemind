# Havemind 1.4.16

Reconnecting a phone no longer brings back notes that were deleted on another
device. Unchanged stale phone files now follow the server's current state,
while a genuine offline edit is still preserved as a conflict.

Privacy: Havemind makes network requests only to the self-hosted server URL a
user explicitly configures for a vault. It contains no telemetry or analytics.
